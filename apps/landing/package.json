{
  "name": "@ecommerce/landing",
  "version": "1.0.0",
  "type": "module",
  "description": "Landing page estática para conversão e SEO",
  "scripts": {
    "dev": "live-server src --port=3003 --host=0.0.0.0",
    "build": "npm run copy-files",
    "copy-files": "if not exist dist mkdir dist && xcopy src\\* dist\\ /E /Y && xcopy assets dist\\assets\\ /E /Y",
    "preview": "live-server dist --port=3003 --host=0.0.0.0",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
